{
  "name": "sg-guard-app",
  "version": "1.0.0",
  "description": "SG Guard prototype with a real backend calling the Gemini API for multimodal scam detection.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
